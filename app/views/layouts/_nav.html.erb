<!--Seperate nav bars will be rendered differently depending on if it is a user that had signed in or a guest-->

<!--The left carrot %=> sign means stop reping html and rep ruby(embeded ruby code)-->

<% if !user_signed_in? %>
	<%= render 'layouts/nav_guest'%>
<% else %>
	<%= render 'layouts/nav_user'%>
<% end %>

